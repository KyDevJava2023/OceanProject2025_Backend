spring:
  application:
    name: backend

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/oceanfn?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: ducky1
    password: kydevjava
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    open-in-view: false
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Ho_Chi_Minh

server:
  port: 1099
  servlet:
    context-path: ""

cloudinary:
  cloud_name: dhbc3ecmm
  api_key: 227333598339835
  api_secret: wtMzlm9lXKX-fu4bn0Zo9c8jtK8

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE